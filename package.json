{
  "name": "bootstrap-italia-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "git submodule init && cd bootstrap-italia-next && npm i && npm run build && cd ..",
    "build": "yarn prepare && sass --load-path=. scss/bootstrap-italia-custom.scss:css/compiled/bootstrap-italia-custom.min.css --style=compressed",
    "start": "http-server ."
  },
  "author": "Francesco Zaia",
  "license": "BSD-3-Clause",
  "dependencies": {
    "bootstrap": "^5.1.3"
  },
  "devDependencies": {
    "http-server": "^14.1.0",
    "sass": "^1.32.6"
  }
}
